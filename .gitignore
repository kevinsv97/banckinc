HELP.md
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/
"OBJETIVO: Generar la venta digital de productos del Banco Popular a través del Web View Portal Asesores, dado que no se realizará un proceso de integración directa con el socio. El objetivo incluye disponibilizar un modelo de propensión que contemple todos los productos vigentes en comercialización de Banco Popular y habilitar el ofrecimiento de los productos sugeridos por dicho modelo.BENEFICIOS: Facilitar la venta digital a través de un proceso eficiente y seguro, permitiendo al Banco Popular ofrecer productos personalizados según el modelo de propensión. Además, la implementación de la validación mediante OTP (One Time Password) asegura un proceso de aceptación robusto, con la capacidad de almacenar cada OTP generada para futuras validaciones o reclamaciones. Este enfoque también optimiza la emisión de ventas confirmadas y su procesamiento en sistemas existentes, manteniendo la consistencia en la nomenclatura de archivos y mejorando la experiencia del cliente mediante una activación eficiente de ecosistemas.FACTORES DE RIESGO DE NO IMPLEMENTARSE:
La falta de un proceso de venta digital adecuado podría limitar la capacidad de Banco Popular para ofrecer productos personalizados, afectando negativamente la eficiencia de las ventas y la satisfacción del cliente. Además, la ausencia de un sistema de validación confiable podría resultar en problemas de seguridad y reclamaciones no gestionadas correctamente.FUNCIONALIDAD:
Implementar la venta digital a través del Web View Portal Asesores, asegurando la habilitación del modelo de propensión para todos los productos en comercialización. Incluir la validación de ventas mediante OTP, almacenando cada OTP generada. Emitir las ventas confirmadas con la nomenclatura establecida y activar la experiencia de bienvenida y los ecosistemas correspondientes. Finalmente, generar reportes integrales de ventas en Qlickview, considerando las ventas realizadas y confirmadas en comparación con las emisiones."
